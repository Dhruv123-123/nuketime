services:
  leetcode-local:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    volumes:
      # problem data + your progress live outside the container
      - ./data:/app/data
    restart: unless-stopped
